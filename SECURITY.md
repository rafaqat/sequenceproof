# Security policy

Do not report vulnerabilities in public issues. Until the repository exists and its private GitHub
Security Advisory channel is verified, do not publish this package. The SequenceProof Engine is
privileged test infrastructure and must never be enabled in production.
